import React from 'react'

export type TabData={
    heading: string
    description: string
}
export const TabComponent = (tabData:TabData[]) => {
    return (
        <div>
          {/* map array with tab heading and tab data*/ }
        </div>
    )
}
